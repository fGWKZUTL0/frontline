<p style="color: green"><%= notice %></p>

<h1>Medical records</h1>

<div id="medical_records" class="grid grid-cols-1 sm:grid-cols-3 gap-2">
  <% @medical_records.each do |medical_record| %>
    <%= render medical_record %>
  <% end %>
</div>
